{
  "name": "restgraphql",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "tsnd --transpile-only --exit-child --ignore-watch node_modules --respawn src/server.ts",
    "knex:migrate": "knex migrate:latest --knexfile knexfile.ts migrate:latest"
  },
  "dependencies": {
    "apollo-server": "^2.19.0",
    "bcrypt": "^5.0.0",
    "knex": "^0.21.12",
    "merge-graphql-schemas": "^1.7.8",
    "sqlite3": "^5.0.0"
  },
  "devDependencies": {
    "ts-node-dev": "^1.0.0",
    "typescript": "^4.0.5",
    "@types/bcrypt": "^3.0.0"
  }
}
